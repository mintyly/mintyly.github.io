---
const { title, date, categories } = Astro.frontmatter;
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{title}</title>
    <meta name="description" content={`Blog post about ${title}`} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body { font-family: sans-serif; max-width: 800px; margin: 2rem auto; line-height: 1.6; }
      h1, h2, h3 { color: #222; }
      pre { background: #f5f5f5; padding: 1rem; overflow-x: auto; border-radius: 5px; }
      code { font-family: monospace; background: #eee; padding: 2px 4px; border-radius: 3px; }
      p.categories { font-style: italic; color: #555; }
    </style>
  </head>
  <body>
    <article>
      <h1>{title}</h1>
      <p>{date}</p>
      {categories?.length > 0 && (
        <p class="categories">Categories: {categories.join(', ')}</p>
      )}
      <hr />
      <slot /> <!-- Markdown content will render here -->
    </article>
  </body>
</html>
